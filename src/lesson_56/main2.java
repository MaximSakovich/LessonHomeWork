package lesson_56;

public class main2 {
 /*  "Обменный пункт валюты" -

           1. Модели (Model)
    User
* Поля: userId, name, email, password
* Методы: getName(), getEmail(), getPassword(), setName(String name), setEmail(String email), setPassword(String password)

    Account
* Поля: accountId, userId, currency, balance
* Методы: getUserId(), getCurrency(), getBalance(), setCurrency(Currency currency), deposit(double amount), withdraw(double amount)

    Transaction
* Поля: transactionId, accountId, type, amount, transactionDate
* Методы: getAccountId(), getType(), getAmount(), getTransactionDate()

    Currency
* Поля: currencyCode, name
* Методы: getCurrencyCode(), getName()

    CurrencyRate
* Поля: currencyCode, rate, date
* Методы: getCurrencyCode(), getRate(), getDate()

   */

}
